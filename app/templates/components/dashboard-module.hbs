{{#if showHeader}}
  <header class="module-header">
    {{#if label}}
      <span class="module-label">{{label}}</span>
    {{/if}}
  </header>
{{/if}}
<div class="module-content">
  {{yield}}
</div>
