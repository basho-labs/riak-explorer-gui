{{#if (eq resourceType 'bucket-type')}}
  {{#link-to 'bucket-type' resourceObj.cluster.name resourceObj.name class=linkClass}}
    {{yield}}
  {{/link-to}}
{{/if}}

{{#if (eq resourceType 'bucket')}}
  {{#link-to 'bucket' resourceObj.cluster.name resourceObj.bucketType.name resourceObj.name class=linkClass}}
    {{yield}}
  {{/link-to}}
{{/if}}

