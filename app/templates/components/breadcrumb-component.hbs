<ol class="breadcrumb">
  {{#if isClusterData}}
    <li>{{link-to 'data' 'cluster.data' data.cluster.id}}</li>

    {{#if data.bucketType}}
      <li>
        {{link-to data.bucketType.bucketTypeId 'bucket-type'
        data.cluster.id
        data.bucketType.bucketTypeId}}
      </li>
    {{/if}}

    {{#if data.bucket}}
      <li>
        {{link-to data.bucket.bucketId 'bucket'
        data.cluster.id
        data.bucketType.bucketTypeId
        data.bucket.bucketId}}
      </li>
    {{/if}}

    {{#if data.riakObject}}
      <li>{{data.riakObject.key}}</li>
    {{/if}}
  {{/if}}

  {{#if isClusterOps}}
    <li>{{link-to 'ops' 'cluster.ops' data.cluster.id}}</li>

    {{#if data.node}}
      <li>
        {{link-to data.node.id 'node'
        data.cluster.id
        data.node.id}}
      </li>
    {{/if}}

    {{#if data.logFile}}
      <li>{{data.logFile.fileId}}</li>
    {{/if}}

    {{#if data.configFile}}
      <li>{{data.configFile.fileId}}</li>
    {{/if}}
  {{/if}}

  {{#if isClusterQuery}}
    <li>{{link-to 'query' 'cluster.query' data.cluster.id}}</li>

    {{#if data.searchSchema}}
      <li>{{data.searchSchema.name}}</li>
    {{/if}}

    {{#if data.searchIndex}}
      <li>{{data.searchIndex.name}}</li>
    {{/if}}

    {{#if data.schemaCreate}}
      <li>create schema</li>
    {{/if}}
  {{/if}}
</ol>
